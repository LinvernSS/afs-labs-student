apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-deployment
  namespace: jxu
  labels:
    app: app
spec:
  replicas: 1
  strategy:
    type: Recreate
  minReadySeconds: 20
  selector:
    matchLabels:
      app: app
  template:
    metadata:
      labels:
        app: app
    spec:
      containers:
      - name: app-deployment
        image: harbor.dev.afsmtddso.com/jxu-devsecops/lab@sha256:744554256c395e0dc75742f10d722a6f08da229361d01ca430a649a5bb00b485
        ports:
        - containerPort: 5000
      - envFrom:
        - secretRef:
            name: db-secret
        - configMapRef:
            name: feature-toggle
